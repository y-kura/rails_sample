<br>

<img src="<%= url_for '/test/image/' + @blog.id.to_s %>" width="100%"><br>
<br>

タイトル：<%= @blog.title =%><br>
投稿者：<%= @user.name =%><br>
<br>

<% if @user.id == session[:user_id] %>
<center>
  <%= link_to '削除', '/test/delete/' + @blog.id.to_s, method: :delete, data: { confirm: '削除してもよろしいですか？' }, class: "btn btn-danger" %>
</center>
<% end %>
